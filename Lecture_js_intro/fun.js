function sayHi(){
  console.log("function says hi");
}


sayHi();

// we can call function from anywhere we want

//no class no bullshit needed

let val = sayHi();
console.log(val);
//it will print undefined because nothing is being returned, so val is undefined

